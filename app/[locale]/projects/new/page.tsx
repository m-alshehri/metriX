import Link from "next/link"; import {redirect,notFound} from "next/navigation"; import {createClient} from "@/lib/supabase/server"; import {getDictionary,isLocale} from "@/lib/i18n"; import {createProject} from "../actions";
export default async function NewProjectPage({params,searchParams}:{params:{locale:string};searchParams?:{error?:string}}){
  if(!isLocale(params.locale))notFound(); const locale=params.locale,t=getDictionary(locale),supabase=createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user)redirect(`/${locale}/login`);
  return <main className="flex min-h-screen items-center justify-center bg-zinc-50 px-6"><div className="w-full max-w-xl rounded-[2rem] bg-white p-8 shadow-soft"><Link href={`/${locale}/dashboard`} className="text-2xl font-black text-metrix-900">metriX</Link><h1 className="mt-8 text-3xl font-black">{t.projects.newTitle}</h1><p className="mt-2 text-zinc-500">{t.projects.newDescription}</p>{searchParams?.error&&<div className="mt-5 rounded-2xl bg-red-50 p-4 text-sm font-semibold text-red-700">{t.projects.error}</div>}<form action={createProject} className="mt-7 space-y-4"><input type="hidden" name="locale" value={locale}/><input name="name" required className="w-full rounded-2xl border border-zinc-200 px-4 py-3" placeholder={t.projects.name}/><textarea name="description" rows={4} className="w-full rounded-2xl border border-zinc-200 px-4 py-3" placeholder={t.projects.description}/><div className="flex gap-3"><button className="rounded-full bg-metrix-900 px-6 py-3 font-bold text-white">{t.projects.submit}</button><Link href={`/${locale}/dashboard`} className="rounded-full border border-zinc-200 px-6 py-3 font-bold text-zinc-700">{t.projects.cancel}</Link></div></form></div></main>;
}
